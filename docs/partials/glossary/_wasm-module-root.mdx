---
title: WASM module root
key: wasm-module-root
titleforSort: WASM module root
---

A 32-byte cryptographic hash that uniquely identifies a specific version of Arbitrum's [State Transition Function](/intro/glossary#state-transition-function) (STF), compiled to [WASM](/intro/glossary#wasm) for use in [fraud proofs](/intro/glossary#fraud-proof). It is computed as the Merkle root over the Keccak256 hashes of every module in the compiled prover image — the main STF replay binary plus runtime libraries such as `soft-float`, `host_io`, `user_host`, and `program_exec`.

The same WASM module root is recorded in two places that must stay in sync:

- **On-chain**, as the `wasmModuleRoot` field on the Rollup contract. It is set at deployment and updated by the chain owner via `setWasmModuleRoot`; every [Assertion](/intro/glossary#assertion) records the root used to compute it.
- **Off-chain**, by every [Validator](/intro/glossary#validator), which keeps the corresponding compiled prover image under `target/machines/<root>/`.

Every [ArbOS](/intro/glossary#arbos) upgrade changes the STF, which produces a new WASM module root. After such an upgrade, validators must retain the prover images for any older roots they still need to validate or dispute historical assertions against.

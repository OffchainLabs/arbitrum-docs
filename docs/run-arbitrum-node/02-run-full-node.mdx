---
title: 'How to run a full node for an Arbitrum chain'
description: Prepare the prerequisites and run an Arbitrum full node with Docker on your local machine
sidebar_position: 1
content_type: how-to
---

import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';
import HardwarePartial from '../partials/_hardware-requirements.mdx';
import DAOChainsParameters from './partials/run-full-node/_dao-chains-parameters.mdx';
import OrbitChainsParameters from './partials/run-full-node/_orbit-chains-parameters.mdx';
import DAOChainsExample from './partials/run-full-node/_dao-chains-example.mdx';
import OrbitChainsExample from './partials/run-full-node/_orbit-chains-example.mdx';
import StateHistoryDefaults from './partials/run-full-node/_state-history-defaults.mdx';
import OptionalTxPrecheckerParameters from './partials/run-full-node/_tx-prechecker-parameters.mdx';
import OptionalParameters from './partials/run-full-node/_optional-parameters.mdx';

This how-to walks you through running a full node for an Arbitrum chain on your local machine. It covers both halves of the job: gathering the hardware, endpoints, and configuration you need, then starting the node with Docker.

<VanillaAdmonition type="tip" title="Want the short version?">

To start an Arbitrum One full node with recommended defaults in four steps, follow the [full node quickstart](/run-arbitrum-node/02-quickstart-full-node.mdx). Come back here to run a different chain, choose a state scheme, or tune the node.

</VanillaAdmonition>

<VanillaAdmonition type="info">

If you want to access an Arbitrum chain but do not want to run your own node, see [RPC endpoints and providers](/arbitrum-essentials/reference/node-providers.mdx) for RPC access to fully managed nodes hosted by a third party.

</VanillaAdmonition>

<VanillaAdmonition type="tip" title="Running on Kubernetes or want a more reliable setup?">

This page covers running a node with Docker. If you want to deploy on Kubernetes, or you want a more production-ready setup with monitoring, log signals, and network egress guidance, follow [How to run a full node with Helm on Kubernetes](/launch-arbitrum-chain/run-a-node/run-full-node-with-helm.mdx) instead.

</VanillaAdmonition>

<VanillaAdmonition type="note" title="Other node roles and support policy">

A Nitro node can also run as a <a data-quicklook-from="sequencer">sequencer</a>, <a data-quicklook-from="batch-poster">batch poster</a>, <a data-quicklook-from="validator">validator</a>, <a data-quicklook-from="archive-node">archive node</a>, or <a data-quicklook-from="feed-relay">feed relay</a>. See [How to assign roles to a Nitro node](/run-arbitrum-node/operation/assign-node-roles.mdx) for the flags that define each role.

For the short-term and long-term support policy, see the [Nitro support policy](/run-arbitrum-node/nitro-support-policy.mdx).

</VanillaAdmonition>

## Before you start

Prepare the following information before you run your node for the first time. **Do not skip these items.** Copy them into a notepad or text editor, because you combine them with other commands and configuration options when you start the node.

### Minimum hardware configuration

The following are the minimum hardware requirements to set up a <a data-quicklook-from="arbitrum-nitro">Nitro</a> full node (not archival):

<HardwarePartial />

Keep the following in mind:

- The RAM and CPU minimums suit nodes that handle a limited number of RPC requests. To process many simultaneous requests, increase both the RAM size and the number of CPU cores.
- Single-core performance matters. If the node falls behind and a single core sits at 100%, upgrade to a faster processor.
- The minimum storage requirements grow as the chain grows. Use more than the minimum to run a robust full node. Snapshot extraction needs about 2x the snapshot size in temporary disk space, so plan for that during initial setup.

### Parent chain (L1) client

Your Arbitrum node needs a connection to a parent chain RPC endpoint, such as an Ethereum execution client for Arbitrum One or Nova. Keep your parent chain client up to date. An incompatible L1 client version can crash your Arbitrum node or stop it from syncing. When you upgrade your L1 client, check the [Nitro release notes](https://github.com/OffchainLabs/nitro/releases) for compatibility requirements.

### Recommended Nitro version

:::caution

Although beta and release candidate versions of the Arbitrum Nitro software exist, use only the release version when you run your node. Beta and RC versions are not supported and might lead to unexpected behavior or database corruption.

:::

Latest [Docker image](https://hub.docker.com/r/offchainlabs/nitro-node/tags): <code>@@latestNitroNodeImage=offchainlabs/nitro-node:v3.11.3-beb2108@@</code>

### Choose a state scheme

Nitro stores its state trie using one of two <a data-quicklook-from="state-scheme">schemes</a>: **<a data-quicklook-from="hashdb">HashDB</a>** (the default) or **<a data-quicklook-from="pathdb">PathDB</a>**. Choose before you initialize the database. You cannot switch an existing database—moving between schemes means re-initializing from a snapshot built with the scheme you want.

| Property                      | HashDB (default)                         | PathDB                                     |
| :---------------------------- | :--------------------------------------- | :----------------------------------------- |
| Flag                          | None; Nitro uses HashDB by default       | `--execution.caching.state-scheme=path`    |
| State trie pruning            | Manual and offline, via `--init.prune`   | Automatic and online                       |
| Block validation              | Supported                                | Not supported                              |
| Full node snapshot            | `pruned`, all three DAO-governed chains  | `full-path`, all three DAO-governed chains |
| Archive snapshot              | `archive`, all three DAO-governed chains | `archive-path`, Arbitrum One and Sepolia   |
| Download with `--init.latest` | Supported                                | Not supported; use `--init.url`            |
| Minimum Nitro version         | Any                                      | v3.9.x                                     |

Both schemes have a published full node snapshot, so either one can initialize without syncing from genesis. At comparable dates the two are close in size: on Arbitrum One, the `pruned` HashDB snapshot is about 2.3 TB and the `full-path` PathDB snapshot is about 2.4 TB.

Pick based on how you want to handle <a data-quicklook-from="state-pruning">pruning</a> and whether you need block validation:

- **Choose HashDB** if your node runs the block validator, or if you want the simplest initialization. `--init.latest pruned` downloads and verifies the snapshot for you. You then prune manually, and your node is offline while it does.
- **Choose PathDB** if you want to avoid manual prune cycles. Nitro prunes online, so the node keeps serving RPC and disk use stays within the window you set with `--execution.caching.state-history`. Initialization takes more work: you pass the snapshot URL to `--init.url` yourself.

On archive nodes the case for PathDB is stronger, because it cuts disk use substantially. See [How to run an archive node](/run-arbitrum-node/more-types/01-run-archive-node.mdx).

<VanillaAdmonition type="warning" title="PathDB and block validation">

PathDB cannot validate blocks. If a node requires the block validator, Nitro exits at startup with `path cannot be used as execution.caching.state-scheme when validator is required`.

A default full node is unaffected: it runs the watchtower strategy, which does not require the block validator. Nitro rejects `path` when you set `--node.block-validator.enable=true`, choose a staker strategy other than `Watchtower`, or enable fast confirmation.

</VanillaAdmonition>

### Database snapshots

::::info Snapshots availability

<a data-quicklook-from="database-snapshot">Database snapshots</a> for Arbitrum One, Arbitrum Nova, and Arbitrum Sepolia are available in the [snapshot explorer](https://snapshot-explorer.arbitrum.io/).

Snapshots are published per state scheme, and a snapshot only works with a node using the same scheme:

| Snapshot kind  | Node type | State scheme | Available for               |
| :------------- | :-------- | :----------- | :-------------------------- |
| `pruned`       | Full node | HashDB       | Arbitrum One, Nova, Sepolia |
| `full-path`    | Full node | PathDB       | Arbitrum One, Nova, Sepolia |
| `archive`      | Archive   | HashDB       | Arbitrum One, Nova, Sepolia |
| `archive-path` | Archive   | PathDB       | Arbitrum One, Sepolia       |

Only the HashDB kinds distinguish pruned from unpruned. PathDB prunes as it runs, so every PathDB snapshot is already pruned to its state-history window—there is no separate unpruned variant to publish. The `pruned` kind exists because a HashDB database is pruned manually, and the published HashDB full node snapshots happen to be pruned ones.

`--init.latest` cannot download the PathDB kinds. See [PathDB snapshots](/run-arbitrum-node/reference/nitro-database-snapshots.mdx#pathdb-snapshots).

Database snapshots for other Arbitrum chains may be available at the discretion of the chain's team. Get in touch with them if you want to use a database snapshot for their chains.

::::

Arbitrum One requires a database snapshot on the first run, because the snapshot supplies information from the Classic era. For other chains a snapshot is optional. Supplying one on the first run provides the state and data for that chain up to a specific block, so the node syncs to the head of the chain faster.

The parameters below are a summary. Read the [complete guide](/run-arbitrum-node/reference/nitro-database-snapshots.mdx) if you plan to use snapshots.

- Use `--init.latest <snapshot type>` (accepted values: `archive`, `pruned`, `genesis`) to download the corresponding snapshot from the configured URL.
- Optionally, use `--init.latest-base` to set the base URL that Nitro searches for the latest snapshot.
- Nitro ignores both parameters if a database already exists.
- When you run more than one node, manually download the parts of the snapshot, join them into a single archive, and host it locally for your nodes. See [Downloading the snapshot manually](/run-arbitrum-node/reference/nitro-database-snapshots.mdx#downloading-the-snapshot-manually) for instructions.
- Only snapshots formatted with your node's state scheme are compatible. A PathDB snapshot does not work for a node using HashDB, and the reverse is also true. Each snapshot directory publishes a `metadata.json` file naming its `state_scheme`; check it before you download.

:::tip Which initialization mode should I use?

| Scenario                                        | Recommended Parameter                                                                                                                                                               |
| :---------------------------------------------- | :---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Setting up a new **full node** on **HashDB**    | `--init.latest pruned`                                                                                                                                                              |
| Setting up a new **full node** on **PathDB**    | The `full-path` snapshot with `--init.url`. `--init.latest` does not accept this kind.                                                                                              |
| Setting up a new **archive** node on **HashDB** | `--init.latest archive` (note: the publicly hosted `archive` snapshot is outdated on Arbitrum One and Sepolia)                                                                      |
| Setting up a new **archive** node on **PathDB** | The `archive-path` snapshot with `--init.url`, on Arbitrum One and Sepolia. It is smaller and more current than the HashDB `archive` snapshot. Nova has no `archive-path` snapshot. |
| Reducing disk usage on an **existing** node     | `--init.prune full` (or `minimal`) — only applicable to HashDB; PathDB handles state trie pruning automatically                                                                     |
| Hosting one snapshot for **multiple nodes**     | Download manually, then use `--init.url file:///path/to/archive.tar` on each node                                                                                                   |
| Using a **custom snapshot URL**                 | `--init.url https://your-snapshot-url/archive.tar`                                                                                                                                  |

For more details on snapshot downloading and initialization, see the [complete snapshot guide](/run-arbitrum-node/reference/nitro-database-snapshots.mdx).

:::

:::warning Fusaka upgrade: historical blobs

If you run a beacon node, historical data now lives in <a data-quicklook-from="blob">blobs</a>. To make this transition, see the [Historical blobs for beacon nodes](/run-arbitrum-node/data-availability/beacon-nodes-historical-blobs.mdx) guide.

:::

### Required parameters

The list below contains all the parameters you need to configure your node. Select the option that matches the chain you want to run a node for.

<div className="dynamic-content-tabs">
  <Tabs className="tabgroup" defaultValue={null}>
    <TabItem value="dao-governed-chains" label="Arbitrum One, Nova, Sepolia">
      <DAOChainsParameters />
    </TabItem>
    <TabItem value="orbit-chains" label="Arbitrum chains">
      <OrbitChainsParameters />
    </TabItem>
  </Tabs>
</div>

## Run the node

Now that you have the prerequisite information prepared, start your node.

:::warning Caution

If you run more than one node, [run a feed relay](/run-arbitrum-node/data-availability/run-feed-relay.mdx).

:::

:::warning PathDB and `--init.latest`

`--init.latest` accepts only `archive`, `pruned`, and `genesis`, and every snapshot it resolves for those kinds uses HashDB. Adding `--execution.caching.state-scheme=path` to the examples below fails, because the downloaded snapshot's scheme does not match.

PathDB snapshots are published under the separate `full-path` and `archive-path` kinds, which `--init.latest` does not accept. To initialize a PathDB database, pass the snapshot URL to `--init.url` instead. See [PathDB snapshots](/run-arbitrum-node/reference/nitro-database-snapshots.mdx#pathdb-snapshots).

:::

:::warning Docker volume mount

To keep the database across restarts, mount an external volume to **`/home/user/.arbitrum`** inside the Docker container.

Create the host directory **before** you run Docker, for example `mkdir -p /some/local/dir/arbitrum`. Otherwise Docker may create it as `root`, and the container (which runs as UID 1000) cannot write to it. See [Note on permissions](#note-on-permissions) if you hit permission errors.

:::

:::note Node config file

To mount a `node-config.json` file with Docker, use the following command:

```
docker run --rm -it -v /Path/to/mount/arbitrum:/home/user/.arbitrum -v /Path/to/node-config.json:/home/user/.arbitrum/node-config.json -p 0.0.0.0:8450:8450 @@latestNitroNodeImage=offchainlabs/nitro-node:v3.11.3-beb2108@@ --conf.file /home/user/.arbitrum/node-config.json
```

:::

Here is an example of how to run `nitro-node`:

<div className="dynamic-content-tabs">
  <Tabs className="tabgroup" defaultValue={null}>
    <TabItem value="dao-governed-chains-example" label="Arbitrum One, Nova, Sepolia">
      <DAOChainsExample />
    </TabItem>
    <TabItem value="orbit-chains-example" label="Arbitrum chains">
      <OrbitChainsExample />
    </TabItem>
  </Tabs>
</div>

Two more things to watch for:

- If you run a node for the parent chain (such as Ethereum for Arbitrum One or Nova) on localhost, add `--network host` right after `docker run` to use Docker host-based networking.
- When you shut down the Docker image, allow a graceful shutdown so the node saves the current state to disk. The following command shuts down all running Docker images gracefully:

  ```shell
  docker stop --time=1800 $(docker ps -aq)
  ```

### Important ports

| Protocol          | Default port |
| :---------------- | :----------- |
| `RPC`/`http`      | `8547`       |
| `RPC`/`websocket` | `8548`       |
| `Sequencer Feed`  | `9642`       |

The `RPC`/`websocket` protocol requires you to enable some ports. Use the following flags:

- `--ws.port=8548`
- `--ws.addr=0.0.0.0`
- `--ws.origins=\*`

### Note on permissions

The Docker image runs as non-root UID 1000. On Linux or macOS, if you get permission errors when you run the Docker image, run the following commands to allow all users to update the persistent folders:

```shell
mkdir /data/arbitrum
chmod -fR 777 /data/arbitrum
```

## Configure and tune the node

### Watchtower mode

By default, the full node runs in Watchtower mode. It watches the onchain assertions and, if it disagrees with them, logs an error containing the string `found incorrect assertion in watchtower mode`.

- On a BoLD-enabled chain such as Arbitrum One or Arbitrum Nova, if you run Nitro before v3.6.0, set the `--node.bold.enable=true` flag so your node monitors onchain assertions properly.
- Setting this flag is optional. Your node continues to operate correctly, validate the Arbitrum One or Nova chain, and serve RPC requests as usual, regardless of this flag.
- Watchtower mode adds a small amount of execution and memory overhead. To deactivate this mode, use the parameter `--node.staker.enable=false`.
- For details on watchtower mode alongside the other validator strategies (`defensive`, `stakeLatest`, `resolveNodes`, `makeNodes`), see [How to run a validator](/run-arbitrum-node/more-types/02-run-validator-node.mdx).

### Pruning

Pruning removes older, unnecessary state from the local copy of the chain your node maintains. It saves disk space and slightly improves the node's efficiency. How you prune depends on the [state scheme](#choose-a-state-scheme) you chose.

On **HashDB**, the default, pruning is a _manual, opt-in_ operation. It removes all states from blocks older than the latest 128. You decide when to run it, and the node stops serving RPC requests until it finishes. On a chain the size of Arbitrum One, that can take days.

On **PathDB**, pruning is automatic and runs online. Nitro discards state older than the `--execution.caching.state-history` window as it goes, so you never schedule a prune and the node never goes offline to run one. Disk use stays within that window instead of growing between manual prunes.

<StateHistoryDefaults />

:::note

Pruning runs when the node starts, during initialization. The node does not serve RPC requests while it prunes.

:::

If you use the default storage scheme (HashDB), activate pruning with the following parameter:

- `--init.prune <pruning mode>`, where `<pruning mode>` can be one of:
  - `minimal`: The most aggressive prune. It retains only the genesis state and the head state (at the latest snapshot), and takes the least amount of time to complete. Duration depends on the chain and database size: several hours for smaller chains, potentially days for large chains like Arbitrum One.
  - `full`: Mostly intended for full nodes serving RPC requests. This mode retains the genesis state, the state of the latest confirmed block, and the head state (at the latest snapshot). It does not work if the node is in `validator` mode. Duration varies significantly by chain and database size—for Arbitrum One, this may take multiple days on NVMe SSDs. For smaller chains, it is much faster. If pruning takes too long, download a fresh pruned snapshot with `--init.latest pruned` instead.
  - `validator`: Meant for validator nodes, and requires an RPC URL for L1 Ethereum. This mode retains the genesis state, the state of the latest confirmed block, the latest confirmed assertion root (obtained from L1), the last locally validated block root, and the head state (at the latest snapshot). This mode takes longer than `full` pruning mode. For validator-specific setup details, see [How to run a validator](/run-arbitrum-node/more-types/02-run-validator-node.mdx).

### Memory management

Under heavy RPC load, or during operations such as large `debug_traceBlockByNumber` calls, Nitro nodes can consume significant memory. To prevent out-of-memory (OOM) crashes, configure the following:

- `--node.resource-mgmt.mem-free-limit`: Declines incoming RPC requests when free system memory drops below the specified threshold, for example `--node.resource-mgmt.mem-free-limit=4GiB`. This protects the node from OOM under high request load.
- `GOMEMLIMIT` environment variable: Sets a soft memory limit for the Go runtime garbage collector, for example `GOMEMLIMIT=48GiB` for a 64 GB machine. This reduces memory spikes.

For an in-depth breakdown of Nitro's memory allocators, cache tuning, and OOM mitigations, see [node tuning and monitoring](/run-arbitrum-node/operation/node-tuning-and-monitoring.mdx).

:::tip

For Docker deployments, set these in your `docker run` command:

```shell
docker run ... -e GOMEMLIMIT=48GiB ... offchainlabs/nitro-node:... --node.resource-mgmt.mem-free-limit=4GiB ...
```

:::

### Transaction prechecker

The transaction prechecker adds extra checks before your node forwards `eth_sendRawTransaction` to the Sequencer endpoint. The flags to set up the prechecker are below:

<OptionalTxPrecheckerParameters />

### Optional parameters

Below are the most commonly used parameters when you run a node. You can also use the flag `--help` for a comprehensive list of the available parameters.

<OptionalParameters />

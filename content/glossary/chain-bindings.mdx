---
id: chain-bindings
title: 'Chain bindings'
---

Software that calls the onchain contracts, and sends transactions to them, so a validator can take part in the [challenge protocol](/docs/glossary#challenge-protocol). Arbitrum uses go-ethereum's `abigen` utility to generate Go bindings for these contracts, plus a few developer-friendly wrappers.

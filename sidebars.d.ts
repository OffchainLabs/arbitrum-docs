/**
 * Type declarations for the CommonJS module sidebars.js.
 *
 * The Docusaurus sidebar config is a large, dynamically-composed structure
 * (it merges generated SDK and Stylus sidebars). Consumers such as
 * scripts/find-orphan-pages.ts traverse it structurally, so it is typed loosely.
 */
declare const sidebars: any;

export default sidebars;
